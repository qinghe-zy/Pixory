### v2.8.1 (OTA 热更新) - 图片阅读器与视频播放器性能与转场优化
- **Update Type**: OTA热更新
- **Version**: 2.8.1
- **Update Time**: 2026-08-19
- **Target Channel**: production
- **Update Group ID**: 2152aeeb-42f3-46e2-b7ef-06fe0d20b164

**更新内容**：
1. **图片阅读器防闪烁优化**：大幅提升了横向和竖向滑动的预渲染窗口范围（`windowSize` 和 `initialNumToRender`），并在底层剥离了对当前索引（`activeIndex`）的闭包依赖，有效避免了在用户极速滑动图片时出现的短时白屏和丢帧。
2. **视频播放器全链路转场优化**：使用 `InteractionManager` 重构了上下划动切换视频（Swipe-to-Switch）的进入逻辑，确保下一条视频的渲染工作彻底完成后再启动转场动画，使得连续滑动切换的体验达到原生级别的丝滑。
3. **视频播放器耗电与发热控制**：利用缓存（`useMemo`）对重渲染开销最大的顶部控制栏进行组件隔离。阻断了系统每秒 4 次底层时间刻度更新向上抛射引起的整页强制刷新，大幅减少长时播放时的电池消耗并优化了 UI 帧率。

---
### v2.8.1 (OTA 热更新) - 修复图片阅读器“正在读取原图”重复加载问题
- **Update Type**: OTA热更新
- **Version**: 2.8.1
- **Update Time**: 2026-08-19
- **Target Channel**: production

**更新内容**：
1. **修复图片阅读器重复加载问题**：之前在图片阅读器（ImageViewerScreen）中新增了全局刷新通知，但这意外导致了阅读器自身的 React `useEffect` 循环触发（因为阅读器监听了全局 `refreshToken`），进而使玩家在滑动看图时频繁出现“正在读取原图...”的整页加载。现在已解除对全局 Token 的循环依赖，恢复丝滑浏览。
2. **“最近查看”清除后实时刷新**：补充修复了在“最近查看”列表里点击【清除记录】后，返回“我的”页面时数字没有立刻清零的问题。现在清除动作也会正确上报全局刷新信号了。

---

### v2.8.1 (OTA 热更新) - 首页待整理弹窗持久化关闭 + 我的页面数字实时刷新
- **Update Type**: OTA热更新
- **Version**: 2.8.1
- **Update Time**: 2026-08-19
- **Target Channel**: production
- **Update Group ID**: 7ebd8780-b7bc-48ab-a402-afe3b006c1a2

**更新内容**：
1. **首页待整理弹窗持久化关闭**：点击"×"关闭待整理提醒后，状态持久化写入本地文件，重启 App 不会再次弹出。只有当新导入图片导致待整理数量超过关闭时的阈值，弹窗才会重新出现。
2. **待整理数量实时刷新**：修复了从"待整理"页面全部处理/删除后返回首页，弹窗仍然显示旧数字的问题——现在只要 `refreshKey` 变化就会立即重新查询最新数量。
3. **"我的"页面收藏数 / 最近查看数实时刷新**：在图片查看器（ImageViewerScreen）中点击收藏或滑动切换图片触发查看记录后，会立即通知主应用刷新全局数据令牌，使"我的"页面上的收藏数与最近查看数及时更新，不再需要重启或切出切回才刷新。

---

### v2.8.1 (OTA 热更新) - 进度条UI调优与视频管理优化
- **Update Type**: OTA热更新
- **Version**: 2.8.1
- **Update Time**: 2026-08-19
- **Target Channel**: production

**更新内容**：
1. **UI 细节优化**：调整了导入环形进度条的高度，并齐常规提示 Toast，适配系统安全区域。
2. **视频管理能力升级**：解除了原来针对视频的诸多限制。在导入后的批量管理列表以及普通 IP 内批量管理时，视频现在可以正常选入并展示操作项了。我们修复了底层组件只允许选择图片的写死限制。
3. **视频原生系统保存**：接入并调用了原生的视频保存能力 (`saveNativeVideoToMediaStore`)，现在可以将视频也导出/保存至系统本地相册 (Movies 目录) 中。

---

### v2.8.1 (OTA 热更新) - 环形进度条重构
- **Update Type**: OTA热更新
- **Version**: 2.8.1
- **Update Time**: 2026-08-19
- **Target Channel**: production
- **Commit**: bb3dc63

**更新内容**：
1. **UI 升级**：移除了原有的线性进度条，重构为更具高级感和呼吸感的环形进度条 (Circular Progress)。
2. **组件化设计**：将 CircularProgress 抽离为高内聚、低耦合的独立受控组件，后续可无缝复用于导出、备份等各种需要进度指示的场景。
3. **极致性能**：内部采用 eact-native-reanimated 的 UI 线程动画与 Shared Value，并继续保持 Ref 句柄隔离状态更新，彻底避免 React 渲染卡顿。

# 📦 Pixory 更新日志 (本地)

> 本文档专用于记录每一次的更新情况（包含热更新与正式版本发布），详细列出功能模块的更新条目。此文件仅存本地，不进入版本控制 (GitHub)。

---

## 🚀 v2.8.1 (OTA 热更新)

**【版本信息】**
- **更新类型**：OTA 热更新
- **目标渠道**：production
- **发布时间**：2026-08-19 20:04
- **相关 Commit**：`8b7fd44`

### ⚡ 性能与体验优化
* **导入进度条**：针对大批量图片/视频导入场景，新增了高性能的线性进度指示器。通过组件隔离与 `ref` 局部刷新技术，彻底消除了大规模导入时带来的页面卡顿和重渲染开销，并结合 `tabular-nums` 实现无抖动的数字与百分比同步显示，提供更加丝滑的导入体验。


## 🚀 v2.8.1 (OTA 热更新)

**【版本信息】**
- **更新类型**：OTA 热更新
- **目标渠道**：production
- **发布时间**：2026-08-17 18:30
- **相关 Commit**：`aef1859` 等

### ⚡ 性能与体验优化
* **私密空间**：重构并优化了私密空间中的内存缓存策略及数据库查询效率。
* **玻璃质感定型**：全面对齐“整理”页面的 IP 选择按钮的磨砂玻璃视觉效果。彻底移除了导致性能不稳定和色彩变绿的底层 `dimezisBlurView` 真实模糊渲染引擎，回归 Expo 原生的纯净半透明奶白遮罩，并附加上了极度微弱（0.05 透明度）的深色物理阴影。在保证拖拽不留黑边残影的前提下，通过微弱的投影勾勒出了组件的边缘和悬浮感，实现了最契合当前浅色主题的轻盈奶白玻璃外观。
* **搜索框防拦截**：为搜索框内的原生 `TextInput` 增加事件穿透 (`pointerEvents="none"`)，阻止其隐性拦截触摸事件，实现搜索框拖拽手势的真正零延迟响应。
* **头部交互优化**：扩大了首页头部文本（“晚上好”、“PIXORY”）的物理触摸响应区，并调优了磁吸流体的谐振子物理参数，使得文本与星轨动画的拖拽手感更加 Q 弹、轻盈。同时，移除了搜索框和部分次级标签按钮（全部、最近更新、收藏）的物理磁吸拖拽特性，以进一步提升复杂页面流的滚动和手势性能。

---

## 🚀 v2.8.1 (正式版本发布)

**【版本信息】**
- **更新类型**：正式版本发布
- **发布时间**：2026-08-17
- **基线版本**：基于 2.8.0 的迭代

### ✨ 新特性与优化 (Features & Enhancements)

#### 🔍 搜索体验
* **界面重构**：优化全局搜索历史记录的交互与展示，采用 1:1 双列网格布局，并引入分层分组机制与路由跳转。
* **视觉强化**：为搜索结果和历史记录增加高亮标识与丝滑的动画过渡效果。
* **快捷操作**：界面常驻“查看更多历史”按钮，提升历史信息的触达率。

#### 🎨 视觉与动效 (UI / UX)
* **光影系统**：升级至前沿的双层视差高光算法 (Biaxial Parallax Specular)，显著提升卡片质感。
* **流体交互**：
  * 为首页英文副标题与三星系统注入磁性流体拖动动效 (MagneticLiquidContainer)。
  * 引入 Fluid Interface 的初速度与欠阻尼谐振子物理模型，极大优化卡片拖拽后的回弹手感。
* **界面细节**：首页“待整理”弹窗新增可关闭的叉号按钮，减少对用户的视觉阻碍。

#### 🎬 媒体与播放
* **顺滑翻页**：视频界面实现三屏虚拟分页器，带来极致丝滑的垂直滑动体验。

### 🐛 问题修复 (Bug Fixes)

#### 🎵 音视频播放
* **倍速失真**：引入动态调节的音调保持策略，彻底解决高倍速播放时出现的“电音”或“花栗鼠音”问题。
* **封面闪烁**：摒弃 `absoluteFillObject`，采用显式的 top/height 布局约束，解决垂直切换和播放期间封面的剧烈闪烁与碰撞问题。
* **黑屏淡入**：视频缩略图的捕获时间点从 0 秒延后至 1 秒，有效规避初始黑屏造成的视觉突兀。

#### 🧩 交互与渲染缺陷
* **手势冲突**：将 `HomeLibraryScreen` 的 `FlatList` 替换为 `RNGH` 核心组件，彻底解决子组件流体手势被原生滚动拦截导致的拖拽延迟。
* **视觉瑕疵**：
  * 修复 Android 平台下 transparent 默认插值黑色所导致的薄膜暗角缺陷。
  * 修复高光和环境光容器边缘出现的切割遮罩（分隔块）异常。
* **崩溃问题**：修复 Worklet 工作线程中因访问未定义捕获变量导致的 Reanimated 原生崩溃问题。
* **搜索导航**：修复会话列表的精准过滤以及聊天历史跳转失效的问题。

#### 📱 系统兼容性
* **文件删除**：解决在 Android 11+ 系统中删除对话框无法被正常调起的问题。
* **后台驻留**：修复应用在后台长时间挂起后，切回前台导致界面卡死的问题。

### ⚡ 性能优化 (Performance)
* **长列表渲染**：针对首页长列表，仅为第一张 IP 卡片启用物理交互特效与高光光斑，其余卡片回退至静态渲染，大幅降低 GPU 功耗并提升列表滑动帧率。

### 🔙 回退与降级方案 (Reverts & Fallbacks)
* **播放器回退**：受限于某些未知稳定性问题，视频播放器及相关模块（截图模块、功能矩阵）已回退至 8 月 15 日之前的稳定状态。
* **传感器降级**：为保证在各种 Android 机型上的绝对兼容性，高光响应传感器由 `ROTATION` (旋转) 降级回退至 `GRAVITY` (重力)，解决了部分设备上的卡片变形问题。


