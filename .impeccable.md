## Design Context

### Users
Pixory 面向主要在 Android 手机上整理本地视觉素材的创作者、运营和品牌视觉使用者。核心任务是离线管理 IP 资产，把原图、未来的视频原文件等素材安全导入私有本地存储后，再按 IP、分组、标签、收藏、备注和回收站完成日常查找与整理。

### Brand Personality
冷静、克制、可靠。界面需要让人觉得数据是安全的、整理是高效的、操作是可信的，不做社交感、炫技感或 AI 生成感表达。

### Aesthetic Direction
轻量浅色主题，强调柔和表面、自然背景、克制主色、紧凑信息排布和移动端产品感。保持 clean、calm、premium、practical 的方向，避免厚重阴影、夸张渐变、玻璃质感、装饰性噪音和明显 AI 味。

### Design Principles
1. 所有核心状态都优先传达“本地可靠”和“原始文件安全”，不用夸张视觉吸引注意力。
2. 页面信息密度要高于普通相册，但仍保持清晰层级和留白节奏。
3. 空状态必须像真实产品页，提供清楚说明与下一步动作。
4. 资产相关视图以整齐、克制、真实预览为主，视频资产要清楚显示封面、时长和播放语义，不使用假封面或大面积装饰。
5. 表单和管理页面要像正式移动产品，不像开发测试页。
6. 面向大容量本地资产库设计，列表、筛选、搜索和批量操作要优先考虑性能、可读性和数据安全。
7. **卡片立体感设计**：涉及卡片制作（如角色卡片、选项卡、独立容器）时，须保持一致的突起/立体感设计，具体实现为通过细微阴影（`shadows.sm`）结合微边框（`StyleSheet.hairlineWidth`）和表面底色（`colors.background.surface`）来实现。避免厚重的扁平或纯黑阴影。

### Spacing Rhythm
全局页面和组件优先使用 `src/design/tokens/rhythm.ts` 的语义间距，不在新页面里临时新增裸 `gap`、`rowGap`、`marginTop` 或 `marginBottom` 来控制卡片垂直节奏。

- 页面大区块之间使用 `rhythm.screenSectionGap`。
- 主卡片到列表使用 `rhythm.heroToListGap`。
- 普通入口卡片之间使用 `rhythm.entryCardGap`。
- 内容列表、分组列表、批量管理列表卡片之间使用 `rhythm.listCardGap`。
- 缩略图、紧凑网格、标签 chip 网格之间使用 `rhythm.compactGridGap`。
- 卡片内部内容、字段内容、常规行内间距使用 `rhythm.cardContentGap` / `rhythm.fieldContentGap` / `rhythm.inlineGap`。

### Component Token Policy
新增页面和组件必须默认从 `src/design/tokens/` 取值，不先写局部硬编码样式。

- 间距和垂直节奏使用 `spacing` / `rhythm`。
- 触控尺寸、底部操作高度、卡片 padding、搜索框和缩略图尺寸使用 `metrics`。
- 圆角使用 `radius`，颜色使用 `colors`，文字使用 `typography.textStyles`。
- 只有在现有 token 无法表达真实布局行为时才允许使用字面量，并在代码旁用简短注释说明原因。
