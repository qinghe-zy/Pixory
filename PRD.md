# Pixory 总体产品需求文档（PRD）

## 0. 文档基础

| 项目 | 内容 |
|---|---|
| 产品名称 | Pixory |
| 文档类型 | 总体产品需求文档，稳定版基线 |
| 基线版本 | v2.8.5 |
| 当前迭代区间 | v2.8.5 至 v2.8.6 |
| 适用平台 | Android 优先；兼容 iOS 设计语言 |
| 文档负责人 | 产品与工程共同维护 |
| 文档状态 | 可供研发、设计、测试、运营评审 |
| 依据 | docs/feature-matrix.md、源码实现、版本文档、发布配置 |

### 0.1 文档目标

本文件是 Pixory 稳定版的产品执行契约。它规定产品范围、用户任务、页面和交互、数据字段、状态流转、权限边界、异常路径、非功能目标、埋点和上线处理。研发可据此拆分任务与数据结构，设计可据此绘制页面状态，测试可据此编写正向、边界和异常用例。

本文件不替代实现级技术设计、数据库迁移脚本、接口协议和测试报告；这些文档必须与本文件的约束保持一致。若实现与本文件不一致，应在评审中明确变更并更新修订记录。

### 0.2 修订历史

| 日期 | 文档版本 | 修改人 | 修改内容 |
|---|---|---|---|
| 2026-09-11 | 3.0 | 产品/工程 | 补充范围、字段、规则、异常、验收、非功能与上线要求 |
| 2026-09-11 | 3.1 | 产品/工程 | 根据稳定版实现补充提示词、记忆、分支、材料、Personal、备份和诊断约束 |

### 0.3 阅读约定

- “必须”表示发布阻断条件；“应”表示默认实现，需有明确理由才可偏离；“可以”表示非强制增强。
- 状态、字段和事件名称以英文标识为准，中文描述用于产品沟通。
- “当前实现”表示仓库中已有实现；“部分实现”表示存在代码或页面但尚未满足完整验收；“规划”表示本版本不承诺上线。

## 1. 项目概况

### 1.1 产品定义

Pixory 是 Android 优先、本地优先的陪伴式 AI 应用。核心交互是可持续的角色对话，而不是一次性问答。用户创建或导入角色卡，建立长线程，允许系统在受控范围内使用记忆、IP 和知识库材料，并通过分支、编辑、重试和恢复保持对话可追溯。

传统 IP 图片/视频资产管理是对话材料基础。导入的原图和视频必须复制到应用私有或受管本地目录，缩略图和预览只能作为独立派生文件，不能覆盖、压缩、裁剪或替代原始文件。

### 1.2 业务背景

现有用户需要同时维护角色设定、长期关系、聊天记录、图片视频和知识文档。一次性聊天产品通常存在四类断点：

1. 线程切换或上下文过长后，角色身份和关系连续性丢失。
2. 自动记忆不可见、不可撤销，错误事实会长期污染回答。
3. 本地材料难以授权、检索和引用，用户无法判断回答依据。
4. 流式生成被停止、切后台或网络中断后，消息状态不明确，容易重复生成或丢失。

Pixory 本阶段解决的是“可持续、可控制、可恢复”的本地陪伴闭环，不承诺云同步、开放社交或通用图像生成。

### 1.3 产品目标与指标

以下是版本目标口径。数据基线在真机和生产观测后填入，不得用演示数据冒充达标。

| 目标 | 指标 | 目标值 | 统计口径 |
|---|---|---:|---|
| 提升对话连续性 | 生成终态完整率 | 不低于 99% | 生成任务进入 final_persisted 的比例 |
| 降低恢复成本 | 可恢复生成成功率 | 不低于 95% | 发生中断后最终形成可阅读消息的比例 |
| 保证首屏反馈 | 首字节时间 p95 | 不高于 3 秒 | 请求开始至 first_byte |
| 控制滚动卡顿 | 聊天流式帧率 | 锁底场景不低于 55 FPS | 真机采样，剔除系统级后台 |
| 保证记忆可控 | 记忆撤销生效时间 | 不高于 1 秒 | 操作完成至后续请求不再注入 |
| 保证隐私边界 | Personal 数据串空间泄漏 | 0 | 数据库、文件、检索、日志四层检查 |
| 保证素材安全 | 原始文件内容变化 | 0 | 导入前后 SHA-256 比对 |
| 支持日常使用 | 冷启动可用时间 p95 | 不高于 2.5 秒 | 从启动至主界面可交互 |

### 1.4 非目标

本基线不包含：公共社区、陌生人社交、云相册、默认云端聊天存储、自动公开分享、通用 AI 绘图、复杂标签分类体系、未经授权的跨空间检索、以缩略图替代原图的存储策略。

### 1.5 术语表

| 术语 | 定义 |
|---|---|
| 空间 Space | 数据隔离边界，分为普通空间和 Personal 私密空间 |
| 线程 Thread | 一条可持续的 AI 对话及其消息、分支、摘要、材料授权 |
| 角色卡 Role Card | 角色身份、提示词、首条消息、头像、边界和模型偏好的可复用配置 |
| 角色快照 | 线程开始或分支创建时冻结的角色卡版本 |
| 记忆 Memory | 带来源、范围、置信度和生命周期的结构化事实或偏好 |
| 材料 Material | 可被线程授权使用的本地文档、图片、视频或 IP 内容 |
| IP | 组织角色、视觉身份、主题或创作系列的顶层本地材料单元 |
| 分支 Branch | 从某条消息版本继续的独立消息路径 |
| 上下文装填 | 按稳定指令、记忆、检索内容、历史和当前请求组装提示词的过程 |
| Personal | 需要额外解锁、独立数据库和文件目录的私密空间 |
| 生成任务 | 一次从用户消息到 AI 最终消息的可恢复执行单元 |
| 软删除 | 仅标记 deleted 并进入回收站，物理删除需用户确认 |

## 2. 用户与场景

### 2.1 用户角色

| 角色 | 目标 | 关键约束 |
|---|---|---|
| 长期陪伴用户 | 与固定角色持续交流 | 需要连续上下文、可恢复生成和可控记忆 |
| 角色创作者 | 创建、导入、验证角色行为 | 需要角色快照、版本差异和提示词保护 |
| IP/世界观创作者 | 管理图片、视频、文档并用于对话 | 需要原文件安全、授权范围和引用来源 |
| 高隐私用户 | 保存私密对话和材料 | 需要 Personal 独立存储、解锁和后台保护 |
| 技术与运营人员 | 诊断失败、性能和成本 | 需要脱敏事件、生成链路和版本维度指标 |

### 2.2 用户故事

| 编号 | 用户故事 | 验收结果 |
|---|---|---|
| US-01 | 作为长期陪伴用户，我想选择一个角色并在 30 秒内开始对话，以便立即进入熟悉的互动状态 | 角色身份、首条消息和输入框可用；失败时可重试，不产生空线程 |
| US-02 | 作为长期陪伴用户，我想在切后台或网络中断后继续未完成回答，以便不丢失上下文 | 返回应用后显示恢复状态；只允许一次续接；最终消息不重复 |
| US-03 | 作为角色创作者，我想导入角色卡并预览解析结果，以便确认提示词和开场白没有被破坏 | 支持声明版本；未知字段保留原文；解析失败不覆盖已有角色 |
| US-04 | 作为用户，我想查看、编辑、撤销和标记记忆，以便控制角色对我的长期认知 | 记忆显示来源、范围、状态；撤销后下一次请求不再注入 |
| US-05 | 作为 IP 创作者，我想把指定材料授权给线程并看到引用，以便判断回答依据 | 未授权材料不得进入检索；引用包含名称、片段和版本定位 |
| US-06 | 作为用户，我想编辑历史消息并从该处创建分支，以便比较不同表达路径 | 原分支只读保留；新分支从目标版本继续；分支之间不串写 |
| US-07 | 作为高隐私用户，我想锁定 Personal 空间并导出完整备份，以便保护和迁移私密数据 | 锁定后禁止查询和后台生成；备份包含数据库、原文件、材料和清单 |

### 2.3 关键场景

1. 通勤单手聊天：输入区固定在底部，流式文本不跳动；用户可停止并稍后续接。
2. 长线程回顾：用户搜索关键词或跳转分支，查看消息来源、收藏和引用。
3. 角色维护：用户编辑角色卡后，新线程使用新快照，旧线程继续使用原快照。
4. 材料问答：用户选择 IP 或知识库，系统只检索授权范围，回答中显示引用。
5. 私密会话：用户进入 Personal 后解锁；应用切后台、锁屏或超时后重新锁定。

## 3. 范围与版本边界

### 3.1 本基线必须交付

- 线程创建、消息发送、流式接收、停止、重试、续接、收藏、编辑、搜索。
- 角色卡创建、编辑、导入、首条消息、头像和会话级快照。
- 记忆检索、状态治理、用户查看、编辑、撤销、删除和作用域限制。
- 分支创建、消息版本、分支路由和分支切换。
- AI 文档、IP、图片视频材料的授权、检索、引用和本地安全导入。
- Personal 空间的数据库、文件、缓存和任务隔离。
- SQLite 本地持久化、备份清单、恢复校验和软删除。
- Provider 配置、密钥保护、诊断事件和基础性能指标。

### 3.2 本基线不承诺

- 多设备实时同步和云端冲突合并。
- 自动把所有对话内容写入全局记忆。
- 默认对私密陪伴回复启用语义缓存。
- 没有来源证据时强行生成确定性事实。
- 未经过真机基线验证的性能数字。

## 4. 业务流程与状态

### 4.1 发送消息主流程

~~~mermaid
flowchart TD
  A[用户输入消息] --> B{线程和空间可用?}
  B -- 否 --> B1[提示解锁或恢复线程]
  B -- 是 --> C[本地保存用户消息]
  C --> D[冻结角色快照与分支路由]
  D --> E[检索记忆与授权材料]
  E --> F[按上下文预算组装提示词]
  F --> G[创建生成任务占位消息]
  G --> H[调用 Provider 并接收流]
  H --> I{流是否中断?}
  I -- 否 --> J[持久化最终消息]
  I -- 是 --> K[记录可恢复状态]
  K --> L{用户返回且租约有效?}
  L -- 是 --> M[一次续接或显示部分结果]
  L -- 否 --> N[标记失败并保留部分内容]
  J --> O[更新摘要、用量和诊断]
  M --> O
  N --> O
~~~

### 4.2 生成任务状态机

~~~mermaid
stateDiagram-v2
  [*] --> created
  created --> local_persisted: 用户消息和占位消息落库
  local_persisted --> request_started
  request_started --> first_byte
  first_byte --> reasoning
  first_byte --> answer
  reasoning --> answer
  answer --> terminal: 完成/停止/错误
  terminal --> final_persisted
  final_persisted --> ui_stable
  request_started --> recovery_pending: 网络/进程/路由中断
  first_byte --> recovery_pending: 网络/进程/路由中断
  answer --> recovery_pending: 网络/进程/路由中断
  recovery_pending --> request_started: 租约有效且允许续接
  recovery_pending --> terminal: 租约失效或已续接
  terminal --> [*]
  ui_stable --> [*]
~~~

### 4.3 导入流程

~~~mermaid
flowchart LR
  A[选择文件] --> B[校验数量、大小、权限]
  B --> C[复制原始文件至私有目录]
  C --> D[读取媒体元数据和哈希]
  D --> E[生成独立缩略图]
  E --> F[SQLite 写入记录]
  F --> G[分配 IP、分组和标签]
  G --> H[返回批次结果]
  F -.失败.-> I[保留 staging 清单待清理]
  E -.失败.-> J[保留原图并标记 previewPending]
~~~

### 4.4 Personal 解锁流程

~~~mermaid
sequenceDiagram
  participant U as 用户
  participant UI as 客户端
  participant S as SecureStore
  participant DB as Personal数据库
  U->>UI: 进入 Personal
  UI->>S: 读取密钥材料
  S-->>UI: 返回解锁结果
  UI->>DB: 打开独立连接
  DB-->>UI: 返回空间状态
  UI-->>U: 展示私密内容
  U->>UI: 锁定或切后台
  UI->>DB: 关闭连接并清理查询缓存
  UI-->>U: 返回锁定态
~~~

### 4.5 线程状态

| 状态 | 进入条件 | 可执行操作 | 禁止操作 |
|---|---|---|---|
| active | 创建成功且空间已解锁 | 发送、编辑、分支、收藏、搜索 | 无 |
| generating | 存在未终止生成任务 | 停止、离开、查看局部内容 | 同一线程再次创建并发生成 |
| recovery_pending | 生成中断且租约未失效 | 返回、续接、停止 | 无限重试 |
| archived | 用户归档 | 查看、恢复、导出 | 直接发送，需先恢复 |
| deleted | 软删除 | 回收站恢复或清除 | 普通列表展示 |

## 5. 功能需求总表

| 编号 | 模块 | 状态 | 责任边界 |
|---|---|---|---|
| FR-01 | 线程与消息 | 已实现/待真机验证 | 聊天主流程和历史 |
| FR-02 | 角色卡与会话身份 | 已实现/部分实现 | 角色配置和快照 |
| FR-03 | 记忆治理 | 已实现/部分实现 | 检索、状态和用户控制 |
| FR-04 | 材料、IP 与引用 | 已实现/部分实现 | 授权、检索和原文件 |
| FR-05 | 分支与消息版本 | 已实现 | 可追溯对话路径 |
| FR-06 | 陪伴运行时 | 部分实现 | 日记、梦境、思绪等后台任务 |
| FR-07 | 流式生成与恢复 | 已实现/待真机验证 | 状态机、续接和持久化 |
| FR-08 | Personal 隔离 | 已实现/待安全审计 | 密钥、数据库、文件和缓存 |
| FR-09 | 媒体导入与资产管理 | 已实现/部分实现 | 原图视频、缩略图、回收站 |
| FR-10 | 搜索、分页与浏览 | 已实现/部分实现 | 长列表和检索体验 |
| FR-11 | 备份与恢复 | 已实现/待恢复演练 | Manifest V2 和校验 |
| FR-12 | Provider 与模型配置 | 已实现/部分实现 | 密钥和路由 |
| FR-13 | 诊断、更新与运营 | 已实现/部分实现 | 事件、版本和发布支持 |

## 6. 功能需求详情

### FR-01 线程与消息

#### 6.1.1 页面与入口

| 页面/组件 | 入口 | 数据来源 |
|---|---|---|
| 线程列表 | 主导航“聊天” | SQLite thread 表，按 updatedAt 倒序 |
| 线程详情 | 点击线程 | thread、message、branch、roleSnapshot |
| 消息操作菜单 | 长按消息或更多按钮 | 当前用户权限和消息状态 |
| 搜索页 | 线程列表搜索按钮 | 本地索引；Personal 仅查询 Personal 数据库 |

#### 6.1.2 交互契约

1. 创建线程必须先确定空间、角色卡和上下文类型；任一必需项失败不得创建半成品线程。
2. 点击发送后先本地保存用户消息，再创建 AI 占位消息和 generation 记录；网络请求不能早于本地落库。
3. 发送按钮在空输入、生成中或空间锁定时不可用。重复点击只允许生成一个任务。
4. 流式文本按增量渲染；用户滚动离开底部时不强制抢回滚动位置，显示“回到底部”入口。
5. 停止生成后保留已接收内容，状态为 stopped；用户可继续生成，但必须创建新的 generation。
6. 编辑历史用户消息必须从目标消息版本创建新分支；原分支内容只读保留。
7. 删除消息使用软删除，默认不物理删除附件和引用记录。

#### 6.1.3 关键字段

| 字段 | 来源 | 类型/约束 | 空值处理 |
|---|---|---|---|
| threadId | 本地生成 | UUID，必填 | 不生成则阻止保存 |
| space | 会话选择 | normal 或 personal | 默认 normal；Personal 未解锁不可选 |
| roleSnapshotId | 创建线程时复制 | UUID，必填 | 角色卡失效时提示重新选择 |
| contextType | 用户选择 | normal、ip、knowledge_base | 默认 normal |
| branchId | 当前路由 | UUID，必填 | 根分支自动创建 |
| messageId | 本地生成 | UUID，必填 | 冲突时重新生成并记录 |
| role | system、user、assistant、tool | 枚举 | 未知值拒绝写入 |
| content | 用户或模型 | UTF-8 文本；单消息建议不超过 200,000 字符 | 空用户消息禁止；空 assistant 仅允许占位 |
| status | 本地状态机 | draft、streaming、complete、stopped、failed、deleted | 默认 draft |
| createdAt/updatedAt | 本地时钟 | ISO 时间 | 写入失败视为事务失败 |
| sourceRefs | 检索服务 | JSON 数组 | 无引用写空数组，不写伪来源 |

#### 6.1.4 业务规则

- 列表默认按 updatedAt 倒序；归档和 deleted 不进入默认列表。
- 同一 space 加 threadId 只能有一个 active generation。
- 消息版本采用 append-only；编辑不覆盖原消息正文。
- 搜索结果必须返回 threadId、messageId、片段和命中位置；用户无权访问的空间不返回结果。

#### 6.1.5 异常路径

| 异常 | 用户反馈 | 系统处理 |
|---|---|---|
| 数据库写入失败 | Toast“暂时无法保存，请重试” | 不发起网络请求，记录 db_write_failed |
| 网络超时 | 消息显示“等待恢复” | generation 进入 recovery_pending |
| Provider 拒绝 | 显示可读错误和重试入口 | 保留用户消息，不自动重复扣费 |
| 应用被杀 | 返回后显示未完成消息 | 按租约尝试一次续接 |
| 重复 delta | 用户界面不重复 | 依据 generation 序号去重 |

#### 6.1.6 验收标准

- 新建线程后立即杀进程，重新进入仍可见用户消息和占位消息。
- 连续快速点击发送只产生一个 generation。
- 编辑历史消息后，旧分支内容和新分支内容均可查看。
- 搜索不能返回另一个空间的消息。

### FR-02 角色卡与会话身份

#### 6.2.1 功能

角色卡包含名称、描述、系统提示词、首条消息、备用问候、头像、标签、边界模式、默认语言和模型偏好。角色卡修改只影响之后创建的快照，不追溯修改已存在线程。

#### 6.2.2 导入

- 支持现有 SillyTavern V2/V3 JSON 结构及常见字段映射。
- 未识别字段写入 extensions.raw，禁止静默丢弃。
- 宏和动态文本先经过安全解析；不允许导入内容改变空间权限、系统策略或缓存键。
- 文件解析失败时保留原文件，不覆盖同名角色。

#### 6.2.3 字段与校验

| 字段 | 类型/长度 | 校验 | 默认值 |
|---|---|---|---|
| name | 字符串，1 至 80 | 去除首尾空白，不得仅为符号 | “未命名角色” |
| description | 文本，最多 20,000 字符 | UTF-8，允许换行 | 空 |
| systemPrompt | 文本，最多 100,000 字符 | 保存前展示变更摘要 | 空 |
| firstMessage | 文本，最多 20,000 字符 | 允许为空 | “你好，我在这里。” |
| alternateGreetings | 文本数组，最多 20 条 | 单条最多 20,000 字符 | 空数组 |
| avatarPath | 本地路径 | 必须位于受管目录 | 使用默认头像 |
| boundaryMode | 枚举 | safe、balanced、custom | safe |
| preferredModel | Provider 模型标识 | 必须通过当前配置解析 | 使用全局默认 |

#### 6.2.4 验收标准

- 创建线程后修改角色卡，当前线程提示词仍使用创建时快照。
- 导入含未知字段的角色卡后，未知内容可在编辑页查看或导出。
- 角色提示词永远位于稳定提示层，动态用户文本不得覆盖其优先级。

### FR-03 记忆治理

#### 6.3.1 记忆模型

记忆不是文本垃圾桶，而是带作用域、来源、置信度、车道、状态和生命周期的产品数据。

| 维度 | 允许值 |
|---|---|
| scope | global、role、ip、knowledge_base、thread、branch |
| lane | confirmed、working、archive |
| status | tentative、committed、confirmed、stale、superseded、conflicted、suppressed、deleted |
| usage | ask_before_action、assert、hedge |
| governance | safety_pending 或普通 |

#### 6.3.2 检索与注入

- retrieval-v1 最多生成 20 个候选，默认最多注入 6 条，基础阈值 0.55。
- 先按空间和作用域过滤，再进行关键词或混合相似度排序。
- 记忆注入位置位于稳定应用规则之后、动态历史之前；禁止把用户消息直接拼进稳定前缀。
- safety_pending、suppressed、deleted 不得注入；stale 只能以不确定语气注入。
- 需要用户确认的记忆不得直接执行外部动作。

#### 6.3.3 用户操作

用户可查看来源消息、创建时间、作用域、状态和最近使用时间；可编辑正文、变更作用域、标记过期、撤销或删除。撤销操作必须立即提升 memory epoch，使后续缓存失效。

#### 6.3.4 异常路径

- 冲突事实同时命中时，显示不确定性并保留冲突记录，不擅自选择新值。
- 向量服务超时，退化到关键词检索并记录 retrieval_degraded。
- 记忆解析疑似包含提示注入时进入 safety_pending，等待用户审核。

### FR-04 材料、IP 与引用

#### 6.4.1 材料授权

线程创建或设置页可授权 IP、知识库、单个文档或媒体集合。授权记录必须包含 space、threadId、materialId、documentVersion 和 enabled。未授权材料不可参与检索，即使文件存在于设备中。

#### 6.4.2 检索

检索采用关键词和可选向量混合策略。向量调用超时上限为 250 毫秒；超时后使用关键词结果。结果需按授权范围、文档版本和删除状态过滤。

#### 6.4.3 引用字段

| 字段 | 说明 |
|---|---|
| label | 用户可读材料名称 |
| text | 命中的原文片段，必要时截断 |
| locator | 页码、段落、时间码或文件内位置 |
| documentVersion | 生成引用时的版本 |
| materialId | 本地材料标识 |

回答显示引用入口；引用文件缺失时显示“来源文件不可用”，不得伪造链接。

### FR-05 分支与消息版本

#### 6.5.1 创建规则

从任意用户或 assistant 消息创建分支。分支记录目标 messageVersionId、父 branchId、创建时角色快照、摘要版本、材料授权快照和 memory epoch。

#### 6.5.2 路由规则

- 当前路由只能指向一个 branch。
- 分支切换只改变读取路径，不复制原消息。
- 分支生成使用创建时快照；后续全局记忆变化只能通过显式策略进入，不得静默改写分支历史。
- 分支删除使用软删除；存在子分支时先标记不可见，再由回收站处理。

#### 6.5.3 验收标准

- 同一消息创建两个分支，分别发送内容后互不出现对方消息。
- 切回旧分支可读到原始回答和引用。
- 分支恢复时若材料版本已删除，提示缺失并继续使用剩余上下文。

### FR-06 陪伴运行时

#### 6.6.1 功能边界

陪伴运行时负责在用户授权和可用证据范围内维护摘要、日记、梦境、思绪和关系事件。它是辅助能力，不得阻塞用户主动发送消息，不得绕过空间锁定，不得把内部情绪数值宣称为客观心理结论。

#### 6.6.2 任务规则

| 任务 | 触发 | 数据范围 | 失败处理 |
|---|---|---|---|
| 摘要压缩 | 历史达到阈值或用户请求 | 当前线程和分支 | 保留旧摘要，稍后重试 |
| 日记生成 | 用户开启且满足周期 | 指定线程/角色 | 写入待处理状态，不生成空内容 |
| 梦境/思绪 | 用户开启且运行时允许 | 角色和授权记忆 | 无证据时跳过，不补写事实 |
| 关系事件 | 明确事件或用户确认 | 当前线程 | 记录来源和时间，不自动扩大作用域 |

#### 6.6.3 验收标准

- Personal 锁定后任何后台任务都不能读取其数据库或文件。
- 后台任务失败不会影响用户发送消息。
- 自动产出的记忆默认为 working 或 tentative，不能直接成为 confirmed 全局记忆。

### FR-07 流式生成与恢复

#### 6.7.1 状态与租约

生成顺序必须遵循 created、local_persisted、request_started、first_byte、reasoning/answer、terminal、final_persisted、ui_stable。每个线程只能存在一个 active generation；恢复租约默认 2 分钟。

#### 6.7.2 恢复规则

- 未收到首字节：允许一次完整重试，前提是 Provider 明确未产生输出。
- 已收到部分输出：只允许一次续接；续接请求必须携带已持久化内容摘要或服务端支持的 continuation 标识。
- 合并重复内容时，使用至少 4 个连续字符的重叠校验；无法确认时保留分段，不静默拼接。
- 停止、完成、失败和路由失焦都必须强制 flush。
- 应用恢复后若租约过期，显示部分结果和失败原因，不自动继续计费。

#### 6.7.3 性能与持久化

| 状态 | 目标 |
|---|---|
| 锁底流式渲染 | 目标 60 FPS，单帧处理不超过 16.7 ms |
| 用户离开底部 | 目标 12 FPS，避免无意义布局 |
| 正常持久化 | 每 500 ms 或达到增量阈值 |
| 压力持久化 | 每 1000 ms |
| 强制持久化 | 后台、完成、停止、错误、路由失焦立即执行 |

### FR-08 Personal 空间隔离

#### 6.8.1 隔离层次

Personal 必须同时隔离数据库连接、文件根目录、检索索引、内存缓存、生成任务、日志字段和备份清单。仅在界面隐藏不视为隔离。

#### 6.8.2 密钥和解锁

- 本地凭据、Provider 密钥和解锁材料存入 SecureStore，不进入普通 SQLite、日志或导出诊断。
- 连续失败达到策略阈值后进入冷却；冷却期间不尝试数据库解锁。
- 切后台、锁屏、手动锁定或超时后关闭 Personal 连接并清理内存中的查询结果。
- 跨空间打开 threadId、materialId 或 memoryId 必须拒绝，不允许通过 URL 或缓存绕过。

#### 6.8.3 验收标准

- 使用同一关键词分别在普通和 Personal 搜索，结果集合完全独立。
- Personal 锁定后触发通知、摘要、备份或生成任务，均返回 locked，不读取内容。
- 导出的普通空间清单不包含 Personal 路径、标题、哈希或统计。

### FR-09 媒体导入与资产管理

#### 6.9.1 导入规则

标准流程为：选择文件、校验数量和空间、复制原始文件、读取元数据和哈希、生成独立缩略图、写入 SQLite、分配 IP/分组/标签、返回批次结果。

| 限制 | 值 |
|---|---:|
| 单批数量 | 1000 |
| 单张图片 | 256 MB |
| 单个视频 | 20 GB |
| 单批总量 | 32 GB |
| 未知类型 | 256 MB |
| 最小剩余空间 | 512 MB |
| 并发复制/处理 | 最多 4 个 worker |

#### 6.9.2 元数据

原始路径、缩略图路径、IP、分组、标签、原始文件名、内部文件名、宽高、MIME、大小、收藏、备注、创建/更新时间、最近查看时间和 deleted 状态必须可追溯。

#### 6.9.3 异常路径

- 复制成功、数据库失败：保留 staging 清单，下一次启动可清理。
- 原图成功、缩略图失败：原图记录保留，标记 previewPending。
- 空间不足：停止新任务，不删除已成功文件，返回成功/失败明细。
- 重复文件：依据 SHA-256 提示已存在，可跳过或建立新记录，默认跳过。
- 删除采用软删除；只有用户确认清空回收站才物理删除原图和缩略图。

### FR-10 搜索、分页与浏览

- 长列表使用 cursor 或稳定复合排序分页，禁止用 offset 在高频更新列表中作为唯一游标。
- 默认排序为更新时间倒序；同一时间使用主键稳定排序。
- 搜索必须包含空间、deleted、权限和材料授权过滤。
- 图片/视频预览使用独立缩略图或派生预览；查看原图时读取原始路径。
- 空结果必须提供标题、原因和下一步动作，不显示无意义的空白页面。

### FR-11 备份与恢复

#### 6.11.1 Manifest V2

备份必须包含 SQLite 数据库、原图目录、缩略图目录、AI 文档、聊天附件、角色头像、版本信息和文件清单。清单记录相对路径、类型、大小、SHA-256、space 和 documentVersion。

#### 6.11.2 恢复流程

1. 读取并校验 Manifest 版本。
2. 在临时目录解包，检查路径穿越、空间标识和哈希。
3. 先导入数据库副本，再以事务方式合并记录。
4. 文件缺失或哈希不符时生成恢复报告，不覆盖已有原文件。
5. 全部校验通过后原子替换索引；失败则回滚临时目录。

#### 6.11.3 验收标准

- 备份恢复后 thread、branch、memory、material、role 和原始文件关系保持一致。
- 缺失缩略图不导致原图丢失；恢复后可重新生成预览。
- Personal 备份必须单独标记空间并支持独立恢复。

### FR-12 Provider 与模型配置

| 字段 | 约束 | 处理 |
|---|---|---|
| providerId | 本地配置标识 | 未知标识拒绝请求 |
| modelId | 提供商模型名 | 发送前校验可用性 |
| apiKey | 敏感字符串 | 仅 SecureStore；日志脱敏 |
| endpoint | HTTPS URL | 禁止明文 HTTP，除非开发环境显式开启 |
| temperature 等参数 | 数值范围由 Provider 声明 | 越界时阻止发送并给出范围 |
| timeout | 正整数毫秒 | 默认值由 Provider 策略提供 |

提供商失败必须区分认证、限流、超时、模型不存在和服务异常。切换 Provider 不得改变历史消息、角色快照或记忆作用域。

### FR-13 诊断、更新与运营

- 诊断事件使用结构化 schema，至少包含 appVersion、build、platform、spaceClass、threadId 哈希、generationId 哈希、event、duration、errorCode。
- 禁止记录原始消息正文、API key、Personal 标题、文件绝对路径和完整提示词。
- 诊断本地保留 7 天且最多 20,000 条；导出前再次执行敏感字段扫描。
- 版本更新检查读取官方 update-version.json；展示当前版本、目标版本、变更摘要和下载入口。
- 发布前必须执行类型检查、测试、diff 检查和 clean Gradle 构建；构建失败不得移动当前版本文档。

## 7. 上下文装填与算法约束

### 7.1 提示词分层

提示词按以下顺序装填：stable_app_policy、stable_role、stable_material_rules、stable_tool_definitions、memory_snapshot、history_window、companion_runtime、temporal_open_loops、summary_bridge、user_observation、dynamic_memory、retrieval_context、current_user_message。

稳定内容置于前部，动态内容置于后部。缓存键必须包含 provider、model、promptVersion、memoryEpoch、retrievalVersion、scope 和生成参数；不得把时间戳、请求 ID、随机值或易变检索结果写入可复用稳定前缀。

### 7.2 上下文预算

默认上下文窗口为 512000 token，系统按 70% 的保守比例分配可用预算，并预留 generation reserve。估算器对中日韩文本采用本地估算规则；最终以 Provider 返回为准。

超限时按动态历史、动态检索、受保护记忆、稳定辅助块的顺序裁剪；必须保留 current_user_message 和 summary_bridge，memory_snapshot 仅在无法避免时降级。历史裁剪以完整的用户/助手回合为单位，禁止截断成半句。

### 7.3 记忆检索预算

记忆上下文分为 C0 至 C6 桶，generation reserve 默认取 max(512, 12%)。默认分布为 C0 10%、C1 10%、C2 8%、C3 8%、C5 12%、C6 10%，C4 使用剩余空间。当 C5 预算不足时关闭图扩展和验证器，并限制注入记忆数量。

### 7.4 检索降级

向量检索超时或不可用时退化到关键词和中文 n-gram 检索；每次降级必须产生诊断事件。低置信度结果使用 hedge 语气；需要确认的结果使用 ask_before_action；只有 confirmed 且满足作用域和安全策略的结果才能以 assert 语气使用。

## 8. 数据模型与权限边界

### 8.1 核心实体关系

| 实体 | 关键关系 |
|---|---|
| Space | 拥有 thread、role、memory、material、asset、backup |
| Thread | 属于 Space；拥有多个 Branch 和 Message |
| Branch | 属于 Thread；指向父版本和当前路由 |
| Message | 属于 Branch；可拥有多个 MessageVersion、sourceRefs |
| RoleCard | 可被多个 Thread 生成快照 |
| Memory | 按 scope 关联 Space、Role、IP、KnowledgeBase、Thread 或 Branch |
| Material | 属于 IP 或 KnowledgeBase；有 documentVersion |
| Generation | 属于 Thread 和 Message；记录状态、用量和恢复信息 |

### 8.2 权限规则

1. 所有查询必须显式带 space 条件；服务层不得依赖调用方“记得过滤”。
2. Personal 的 ID、路径和内容不可出现在普通空间错误信息、搜索索引、通知和诊断中。
3. 角色提示词、记忆和材料授权在生成前冻结快照，生成过程中不因 UI 修改而改变。
4. 物理删除需用户明确确认；软删除对象在默认业务查询中不可见。

## 9. 非功能需求

### 9.1 性能

| 项目 | 目标 |
|---|---|
| 冷启动至可交互 | p95 不高于 2.5 秒 |
| 线程列表首屏 | p95 不高于 1.5 秒 |
| 本地发送落库 | p95 不高于 200 ms |
| 检索响应 | 关键词 p95 不高于 300 ms；向量超时 250 ms |
| 首字节时间 | p95 不高于 3 秒 |
| 流式渲染 | 锁底不低于 55 FPS；离底部不低于 12 FPS |
| 批量导入 | 4 worker 并发，UI 不阻塞；进度可恢复 |

所有数字需在目标 Android 机型上采集 p50、p95 和失败样本；没有实测数据时标为待验证。

### 9.2 兼容性

- Android 最低版本以 app.json 和 Gradle 配置为准；稳定版重点验证 Android 10 及以上。
- 需要验证低内存设备、深色模式、字体放大、横竖屏切换、系统返回和后台恢复。
- Android 11 及以上删除外部媒体必须遵循系统确认流程；不绕过系统授权。

### 9.3 安全与隐私

- API key、解锁凭据和敏感 Provider 配置只存 SecureStore。
- 网络默认 HTTPS；证书或域名异常时阻止发送并提供重试。
- 日志、埋点、备份和错误上报均执行敏感字段脱敏。
- Personal 使用独立数据库、文件根目录、索引、缓存和任务队列。
- 导入材料和聊天数据默认本地保存；任何服务端能力必须显式说明范围、保留、删除和缓存策略。

### 9.4 可靠性

- 关键本地写入采用 SQLite 事务和 WAL；迁移必须可重复执行。
- 生成任务必须可恢复、可停止、可重试且不产生重复 assistant 消息。
- 导入和恢复失败须保留可诊断状态，不以“成功”吞掉部分失败。
- 所有异步任务支持取消、超时和幂等键。

### 9.5 可用性与无障碍

- 关键操作提供加载、空、错误、成功和恢复状态。
- 触控目标不小于 44dp；重要状态不能只依靠颜色表达。
- 文字支持系统字体放大，不截断关键错误和隐私提示。
- 返回、关闭和取消操作可预测；破坏性操作需明确确认。

## 10. 埋点与验收

### 10.1 事件

| 事件 | 触发 | 必填属性 |
|---|---|---|
| thread_created | 线程创建成功 | spaceClass、contextType、roleSnapshot |
| message_send_clicked | 用户点击发送 | spaceClass、contentLengthBucket |
| generation_started | 请求开始 | provider、model、promptVersion |
| generation_first_byte | 首字节收到 | latencyMs |
| generation_recovered | 续接成功 | recoveryType、attempt |
| generation_terminal | 生成终止 | terminalState、durationMs、errorCode |
| memory_action | 查看/确认/撤销/删除 | scope、status、action |
| retrieval_completed | 检索完成 | retrievalVersion、candidateCount、injectedCount、degraded |
| material_citation_opened | 用户打开引用 | materialType、documentVersion |
| import_batch_completed | 导入批次结束 | total、success、failed、previewPending |
| personal_locked | Personal 锁定 | trigger |
| backup_completed | 备份完成 | manifestVersion、fileCount、sizeBucket |

禁止属性：原始消息正文、完整提示词、API key、绝对路径、Personal 标题和文件名。

### 10.2 验收矩阵

| 领域 | 正向用例 | 边界/异常用例 | 通过证据 |
|---|---|---|---|
| 线程 | 创建、发送、搜索、收藏 | 重复点击、数据库锁、路由失焦 | 自动化测试和事件链 |
| 角色 | 创建、导入、快照 | 未知字段、宏注入、头像缺失 | 导入快照和提示词快照 |
| 记忆 | 查看、确认、撤销 | 冲突、过期、跨作用域 | 状态变化和注入记录 |
| 材料 | 授权、检索、引用 | 向量超时、版本删除、未授权 | citation 与授权日志 |
| 分支 | 编辑、创建、切换 | 并发编辑、子分支删除 | branch/messageVersion |
| 恢复 | 停止、续接、重试 | 迟到 delta、重复 patch、杀进程 | generation 状态机 |
| Personal | 解锁、锁定、导出 | 连续失败、跨空间、后台任务 | 隔离扫描 |
| 导入 | 原图、缩略图、回收站 | 空间不足、重复文件、取消 | SHA-256 和批次报告 |
| 备份 | 备份、恢复、回滚 | 缺文件、哈希不符、ID 冲突 | Manifest 和恢复报告 |

## 11. 上线与运营

### 11.1 发布前

1. 对照 docs/feature-matrix.md 检查功能状态和变更范围。
2. 执行 pnpm typecheck、pnpm test、git diff --check。
3. Android 构建前执行 gradlew clean，再执行 assembleRelease。
4. 校验 APK 签名、版本号、runtimeVersion 和下载元数据。
5. 在目标真机验证冷启动、流式、后台恢复、Personal 锁定、导入和备份恢复。

### 11.2 发布策略

| 变更类型 | 策略 |
|---|---|
| 纯文案、非关键样式 | 小比例灰度后全量 |
| 线程、记忆、Provider 逻辑 | 先内部和小流量，观察生成完整率与错误率 |
| 数据库迁移、Personal、备份 | 必须先做可恢复演练，再逐步放量 |
| 安全修复 | 评估影响后优先发布，保留回滚路径 |

### 11.3 历史数据处理

- 新字段提供默认值或兼容读取；不得因新增字段让旧线程不可打开。
- 角色卡和材料版本采用向前兼容，旧版本无法解析时保留原始内容。
- 迁移前自动备份数据库；迁移失败恢复原库并输出错误报告。
- 旧软删除数据按回收站策略保留，未经用户确认不物理删除。

### 11.4 上线后监测

首 24 小时重点观察：生成终态完整率、首字节 p95、恢复成功率、Provider 错误率、数据库写入失败、Personal 隔离告警、原始文件哈希异常和崩溃率。达到阻断阈值时暂停扩大灰度并保留现场诊断数据。

## 12. 当前实现状态与关联文档

| 状态 | 判定标准 |
|---|---|
| 已实现 | 代码路径、页面或测试已存在，且符合本 PRD 约束 |
| 部分实现 | 核心路径存在，但仍缺异常处理、真机验证或完整运营闭环 |
| 实验 | 仅在实验模式或内部开关可用，不作为稳定版承诺 |
| 规划 | 已定义方向，尚未纳入当前交付 |
| 待验证 | 实现可能存在，但性能、安全、恢复或兼容性尚未形成证据 |

相关文档：

- 功能矩阵：docs/feature-matrix.md
- 当前版本 Spec、Plan、Review：版本文档/当前版本文档/
- 历史版本归档：版本文档/历史文档/
- 版本发布说明：版本文档/版本更新说明/
- 架构、上下文、记忆、性能和监测报告：版本文档/当前版本文档/

本 PRD 的任何范围、字段、状态或验收变更，必须同步更新修订历史，并在同一评审中通知研发、设计、测试和运营。

## 13. 变更记录
<!-- PIXORY_CHANGE_RECORD -->

| 日期 | 变更类型 | 旧规则 | 新规则 | 原因/影响 | 知会对象 |
|---|---|---|---|---|---|
| 2026-09-11 | 文档治理 | 版本 PRD 仅保存在版本过程目录 | 根目录 `PRD.md` 作为跨版本公开总 PRD，并由打包流程依据当前版本 PRD 同步维护 | 保持公开产品规则与版本迭代连续一致 | 产品、研发、设计、测试、运营 |

## 14. 版本路线图
<!-- PIXORY_ROADMAP_START -->
### 维护状态

- 已归档版本：v2.8.5
- 当前后续迭代：v2.8.6
- 维护来源：当前版本 PRD、TDD、Test Report、功能矩阵和评审变更记录

### 路线内容

- 持续强化陪伴式 AI 聊天的连续性、可恢复生成与长期记忆控制。
- 完善多模型 Provider 的可靠性、数据边界、成本观测和失败降级。
- 推进 Personal 空间、材料授权、知识库检索与备份恢复的真机验证。
- 以用户反馈和版本测试报告为依据，逐版本更新优先级，不把未验证能力包装成稳定承诺。

### 更新规则

- 每次版本迭代和评审后需求变更都要更新本节，不能只追加重复描述。
- 当前有效路线必须与 PRD 的当前有效内容一致；被替代路线移入变更记录或明确标记为历史。
<!-- PIXORY_ROADMAP_END -->
